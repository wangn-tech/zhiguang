<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangning.knowpost.mapper.KnowPostMapper">

    <resultMap id="KnowPostResultMap" type="com.wangning.knowpost.domain.KnowPost">
        <id column="id" property="id"/>
        <result column="tag_id" property="tagId"/>
        <result column="tags" property="tags"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="content_url" property="contentUrl"/>
        <result column="content_object_key" property="contentObjectKey"/>
        <result column="content_etag" property="contentEtag"/>
        <result column="content_size" property="contentSize"/>
        <result column="content_sha256" property="contentSha256"/>
        <result column="creator_id" property="creatorId"/>
        <result column="is_top" property="isTop"/>
        <result column="type" property="type"/>
        <result column="visible" property="visible"/>
        <result column="img_urls" property="imgUrls"/>
        <result column="video_url" property="videoUrl"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="publish_time" property="publishTime"/>
    </resultMap>

    <insert id="insertDraft" parameterType="com.wangning.knowpost.domain.KnowPost">
        INSERT INTO know_posts (
            id,
            creator_id,
            status,
            type,
            visible,
            is_top,
            create_time,
            update_time
        ) VALUES (
            #{id},
            #{creatorId},
            #{status},
            #{type},
            #{visible},
            #{isTop},
            #{createTime},
            #{updateTime}
        )
    </insert>

    <select id="findById" resultMap="KnowPostResultMap">
        SELECT *
        FROM know_posts
        WHERE id = #{id}
    </select>

    <update id="updateContent" parameterType="com.wangning.knowpost.domain.KnowPost">
        UPDATE know_posts
        <set>
            <if test="contentUrl != null">content_url = #{contentUrl},</if>
            <if test="contentObjectKey != null">content_object_key = #{contentObjectKey},</if>
            <if test="contentEtag != null">content_etag = #{contentEtag},</if>
            <if test="contentSize != null">content_size = #{contentSize},</if>
            <if test="contentSha256 != null">content_sha256 = #{contentSha256},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
          AND creator_id = #{creatorId}
    </update>

    <update id="updateMetadata" parameterType="com.wangning.knowpost.domain.KnowPost">
        UPDATE know_posts
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="tagId != null">tag_id = #{tagId},</if>
            <if test="tags != null">tags = #{tags},</if>
            <if test="imgUrls != null">img_urls = #{imgUrls},</if>
            <if test="visible != null">visible = #{visible},</if>
            <if test="isTop != null">is_top = #{isTop},</if>
            <if test="description != null">description = #{description},</if>
            <if test="type != null">type = #{type},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
          AND creator_id = #{creatorId}
    </update>

    <update id="publish">
        UPDATE know_posts
        SET status = 'published',
            publish_time = CURRENT_TIMESTAMP,
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND creator_id = #{creatorId}
    </update>

    <update id="updateTop">
        UPDATE know_posts
        SET is_top = #{isTop},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND creator_id = #{creatorId}
    </update>

    <update id="updateVisibility">
        UPDATE know_posts
        SET visible = #{visible},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND creator_id = #{creatorId}
    </update>

    <update id="softDelete">
        UPDATE know_posts
        SET status = 'deleted',
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND creator_id = #{creatorId}
    </update>

    <select id="listFeedPublic" resultType="com.wangning.knowpost.domain.KnowPostFeedRow">
        SELECT
            p.id,
            p.title,
            p.description,
            p.tags,
            p.img_urls AS imgUrls,
            u.avatar AS authorAvatar,
            u.nickname AS authorNickname,
            u.tags_json AS authorTagJson,
            p.publish_time AS publishTime,
            p.is_top AS isTop
        FROM know_posts p
        JOIN users u ON p.creator_id = u.id
        WHERE p.status = 'published'
          AND p.visible = 'public'
        ORDER BY p.publish_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="listMyPublished" resultType="com.wangning.knowpost.domain.KnowPostFeedRow">
        SELECT
            p.id,
            p.title,
            p.description,
            p.tags,
            p.img_urls AS imgUrls,
            u.avatar AS authorAvatar,
            u.nickname AS authorNickname,
            u.tags_json AS authorTagJson,
            p.publish_time AS publishTime,
            p.is_top AS isTop
        FROM know_posts p
        JOIN users u ON p.creator_id = u.id
        WHERE p.creator_id = #{creatorId}
          AND p.status = 'published'
        ORDER BY p.is_top DESC, p.publish_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="listPublishedIdsByCreator" resultType="long">
        SELECT id
        FROM know_posts
        WHERE creator_id = #{creatorId}
          AND status = 'published'
    </select>

    <select id="findDetailById" resultType="com.wangning.knowpost.domain.KnowPostDetailRow">
        SELECT
            p.id,
            p.creator_id AS creatorId,
            p.title,
            p.description,
            p.tags,
            p.img_urls AS imgUrls,
            p.content_url AS contentUrl,
            p.content_object_key AS contentObjectKey,
            p.content_etag AS contentEtag,
            p.content_sha256 AS contentSha256,
            u.avatar AS authorAvatar,
            u.nickname AS authorNickname,
            u.tags_json AS authorTagJson,
            p.publish_time AS publishTime,
            p.is_top AS isTop,
            p.visible,
            p.type,
            p.status
        FROM know_posts p
        JOIN users u ON p.creator_id = u.id
        WHERE p.id = #{id}
    </select>
</mapper>
