<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangning.relation.outbox.OutboxMapper">

    <insert id="insert" parameterType="com.wangning.relation.outbox.OutboxRecord">
        INSERT INTO outbox (
            id, aggregate_type, aggregate_id, type, payload, created_at
        ) VALUES (
            #{id}, #{aggregateType}, #{aggregateId}, #{type}, CAST(#{payload} AS JSON), #{createdAt}
        )
    </insert>
</mapper>
